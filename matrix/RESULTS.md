# Results Matrix

A catalog of theoretical and empirical results across the trilogy,
with connections between them.

---

## Tier 1: Formally Verified Results (Lean, zero sorries)

### R1. Transformer Implements BP (this repo)
**Statement:** There exist explicit transformer weights W such that
one forward pass on an encoded factor graph state decodes to exactly
one round of belief propagation.